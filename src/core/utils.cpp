// v190.0: All utility functions are defined inline in ml/core/utils.h
// This file is intentionally empty to avoid redefinition errors.
// The header-only approach allows for better inlining optimizations.

#include "ml/core/utils.h"

// Explicit template instantiations or non-inline implementations
// can be added here if needed in the future.
